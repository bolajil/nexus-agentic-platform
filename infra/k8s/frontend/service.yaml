apiVersion: v1
kind: Service
metadata:
  name: nexus-frontend
  namespace: nexus
  labels:
    app: nexus-frontend
spec:
  type: ClusterIP
  selector:
    app: nexus-frontend
  ports:
    - name: http
      port: 80
      targetPort: 3002
